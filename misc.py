def makeAssignment(subject, title, type, date, description):
    # ["Subject", "Title", "Type", "Date", "Description"]
    dic = {}
    dic["Subject"] = subject
    dic["Title"] = title
    dic["Type"] = type
    dic["Date"] = date
    dic["Description"] = description
    return dic